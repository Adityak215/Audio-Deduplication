# Frontend (Next.js)

## Setup

1. Install dependencies:
   ```bash
   npm install
   ```
2. Create `.env.local`:
   ```bash
   NEXT_PUBLIC_API_BASE_URL=http://localhost:5000
   ```
3. Start dev server:
   ```bash
   npm run dev
   ```

Open `http://localhost:3000`.
